\documentclass{standalone}
\usepackage{bubbleplot}

${defineColorsYear}

\pgfplotsset{
  colormap={yearcolor}{
    ${setColorsYear}
  }
}

\begin{document}
\bubbleplot[%
    %% width=,
    %% height=,
    xmin=${xMin},
    xmax=${xMax},
    ylabel=${yLabel},
    meta=${meta},
    x field=${xField},
    %% enlarge y limits=,
    x index field=${xIndexField},
    y field=${yField},
    y index field=${yIndexField},
    year field=${yearField},
    %% year x shift=,
    %% year y shift=,
    %% year padding=,
    x left label=${xLeftLabel},
    %% x left label shift=,
    x right label=${xRightLabel},
    %% x right label shift=,
]{${CSVDataFile}}{${colorsYear}}
