\documentclass{standalone}
\usepackage{bubbleplot}

${define_color_year}

\pgfplotsset{
    colormap={yearcolor}{
        ${color_map_year}
    }
}

\begin{document}
\bubbleplot[%
    %width=,
    %height=,
    xmin=${x_min},
    xmax=${x_max},
    ylabel=${y_facet_label},
    x index field=${csv_col_x_indices},
    y index field=${csv_col_y_indices},
    %x field=, % TODO: fill via Python
    %y field=, % TODO: file via Python
    meta=${csv_col_occurence},
    %year x shift=,
    %year y shift=,
    x left label=${x_left_facet_label},
    %x left label shift=,
    x right label=${x_right_facet_label},
    %x right label shift=,
]{${csv_data_file}}{${color_value_label}}
